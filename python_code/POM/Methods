"""
pom:page object model  以页面为单位，将同一个页面中的测试对象封装在同一个模块中==分层思想
分层的作用：实现代码的复用，实现数据和业务的分离，便于用例的管理和维护
项目中自动化占比：30%-60% （UI自动化占比20%-30%，接口自动化占比70%-80%）==》
    UI自动化中元素变化太大，标签的位置和属性容易发生改变，维护比较麻烦，所以UI只针对一些核心的业务

基础公共类：封装UI自动化的公共方法
业务流程层：以页面为对象，定位元素以及对元素的操作
数据层：存放测试过程中需要使用到的测试数据，登录功能的账号和密码，搜索功能的关键字
用例层：调用业务层的方法，用unittest管理，变成测试用例。再使用ddt（数据驱动测试，data driver test）调用数据层数据进行测试
执行层：执行用例层的用例
报告层：存放执行用例时产生的测试报告，测试日志
工具层：存放第三方模块以及使用的工具
"""
"""
Base模块用于定义一个基础公共方法类
1.元素定位方法：定位元素
2.页面元素的操作：页面访问，输入内容，点击，弹框，窗口切换，鼠标悬停
"""